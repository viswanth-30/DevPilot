# Frontend Environment Configuration Example

# API Base URL (leave empty in development to use Vite proxy, e.g. proxying to http://localhost:8080)
# In production, set this to the actual backend URL, e.g. https://api.devpilot.example.com
VITE_API_BASE_URL=
